{
  "manifest_version": 3,
  "name": "Tab Cycler",
  "description": "Preview and cycle through open tabs with user-configured keyboard shortcuts.",
  "version": "1.2.0",
  "permissions": ["activeTab", "scripting", "tabs"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "src/background.js",
    "type": "module"
  },
  "action": {
    "default_title": "Tab Cycler",
    "default_popup": "src/options.html"
  },
  "options_page": "src/options.html",
  "commands": {
    "cycle-next-tab": {
      "suggested_key": {
        "default": "Ctrl+Shift+Comma",
        "mac": "Command+Comma"
      },
      "description": "Show tab switcher and select the next tab"
    },
    "cycle-previous-tab": {
      "suggested_key": {
        "default": "Ctrl+Shift+Period",
        "mac": "Command+Shift+Comma"
      },
      "description": "Show tab switcher and select the previous tab"
    }
  }
}
